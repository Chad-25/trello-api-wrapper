<h1>Board Details</h1>

<table border='1'>
  <thead>
    <tr>
      <th>ID</th>
      <th>Board Name</th>
      <th>URL</th>
      <th>Permission</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td><%= @board.id %></td>
        <td><%= @board.name %></td>
        <td><%= @board.url %></td>
        <td><%= @board.permission_level %></td>
        <td><%= link_to 'Cards Details', show_cards_user_path(@board.id) %></td>
      </tr>
  </tbody>
</table>

<%= link_to "Main", users_path %>
